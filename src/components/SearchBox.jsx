import React from "react";

const SearchBox = () => {
  return (
    <input
      className="w-full outline-none p-3 text-lg"
      type="search"
      name="search"
      placeholder="Search"
    />
  );
};

export default SearchBox;
